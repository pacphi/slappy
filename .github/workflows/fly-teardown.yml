# Fly.io Teardown Workflow - Destroys the deployed application
# ⚠️  WARNING: This is a destructive operation that cannot be undone

name: Fly Teardown

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to tear down'
        required: true
        type: choice
        options:
          - production
          - staging
      confirm_destruction:
        description: 'Type "DESTROY" to confirm deletion'
        required: true
        type: string

jobs:
  teardown:
    name: Destroy Fly.io deployment
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.environment }}
    steps:
      - name: Validate confirmation
        run: |
          if [ "${{ inputs.confirm_destruction }}" != "DESTROY" ]; then
            echo "❌ Confirmation failed. You must type 'DESTROY' exactly to proceed."
            exit 1
          fi
          echo "✅ Confirmation received. Proceeding with teardown..."

      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Check if app exists
        id: check_app
        run: |
          if flyctl status --app slappy 2>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ App 'slappy' exists and will be destroyed"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "ℹ️  App 'slappy' does not exist"
          fi
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        continue-on-error: true

      - name: Destroy Fly.io app
        if: steps.check_app.outputs.exists == 'true'
        run: |
          echo "🗑️  Destroying Fly.io app 'slappy'..."
          flyctl apps destroy slappy --yes
          echo "✅ App destroyed successfully"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Teardown summary
        run: |
          echo "## Teardown Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** ${{ inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "- **App:** slappy" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** Destroyed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ The deployment has been permanently removed from Fly.io" >> $GITHUB_STEP_SUMMARY
